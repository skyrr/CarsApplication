@model IEnumerable<CarsApplication.Models.Point>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.point1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.point1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.point_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.point_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.point_id })
        </td>
    </tr>
}

</table>
